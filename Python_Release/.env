# =========================
# DropOver Project
# Environment (.env)
# =========================

# Networking / App
PORT=8787
APP_TITLE=Spacedrop Server (one-way)

# Paths
DOWNLOADS_DIR=~/Downloads
CONF_DIR=~/.config/Spacedrop
CONF_PATH=~/.config/Spacedrop/config.json

# Modes (PERSONAL remains a hidden option)
VALID_MODES=EVERYONE,CONTACTS_ONLY,OFF,PERSONAL

# Prompt even when the sender is my own UserID?
# 0 = don't prompt for self (default), 1 = do prompt for self
CONFIRM_ON_SELF=0

# Notifications / Confirmation
# Use a custom icon for notifications & dialogs (PNG/ICNS). Path is ~-expandable.
ICON_PATH=~/Spacedrop/icon.png

# If a sender's UserID is unknown/different than personal_user_id,
# show a blocking Accept/Decline dialog (AppleScript). 1=on, 0=off.
REQUIRE_CONFIRM_ON_FOREIGN=1

# Seconds to wait before the dialog auto-declines (0 = no timeout)
APPROVAL_TIMEOUT=20
