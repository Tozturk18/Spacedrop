# ============================================================================
# Spacedrop .env (for both C and Python implementations)
# ============================================================================

# --- Core server ---
SPACEDROP_PORT=8787
SPACEDROP_THREADS=4
SPACEDROP_KEEP_ALIVE=yes
SPACEDROP_ACCESS_LOG=-
SPACEDROP_ERROR_LOG=-
SPACEDROP_DOCROOT=.

# --- File drop behavior ---
# Where to save actual files (Finder-style enumeration used)
SPACEDROP_DOWNLOADS=~/Downloads
# What to do with non-URL text payloads: clipboard | file | both
SPACEDROP_DROP_TEXT=clipboard
# Basename for saved text files
SPACEDROP_TEXT_BASENAME=Spacedrop Text.txt
# Keep URL wrapper files (.webloc, .txt, .url, .html) after opening? (yes|no)
SPACEDROP_KEEP_WRAPPER=no

# --- Config paths ---
SPACEDROP_CONFIG=~/.config/spacedrop/config.json
SPACEDROP_CONF_DIR=~/.config/spacedrop
SPACEDROP_CONF_PATH=~/.config/spacedrop/config.json

# --- Modes (auth) ---
# Valid modes the server will accept
SPACEDROP_VALID_MODES=EVERYONE,CONTACTS_ONLY,OFF,PERSONAL

# --- Confirmation / security (parity with Python helpers) ---
# Require confirmation on foreign senders (1 = yes, 0 = no)
SPACEDROP_REQUIRECONFIRM_ON_FOREIGN=1
# Show confirmation even for self devices (1 = yes, 0 = no)
SPACEDROP_CONFIRM_ON_SELF=0
# Timeout (in seconds) for confirmation dialogs
SPACEDROP_APPROVAL_TIMEOUT=20

# --- Notifications / GUI ---
# Custom icon path for notifications (used if terminal-notifier is available)
SPACEDROP_ICON_PATH=~/Spacedrop/icon.png

# --- Clipboard ---
# Default push kind if not specified (text|image)
SPACEDROP_CLIP_DEFAULT=text

# --- Debugging / development ---
# Set to 1 to log extra diagnostics
SPACEDROP_DEBUG=0
